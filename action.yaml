name: 'reliably-action'
description: 'Run manifests analysis for reliability issues'
inputs:
  files:
    description: 'Files subject to verification'
    required: false
    default: 'manifest.yaml'
outputs:
  result:
    description: 'Outputs of Reliably'
runs:
  using: 'docker'
  image: 'Dockerfile'